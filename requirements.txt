# Core
streamlit>=1.30.0
groq>=0.5.0
python-dotenv>=1.0.0

# Whisper (transcription locale)
openai-whisper
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# TTS (doublage)
edge-tts>=6.1.9
pydub>=0.25.1
soundfile>=0.12.0
numpy>=1.24.0
tqdm>=4.65.0

# Audio Separation
demucs

# Diarization
simple-diarizer

# Optionnel: Flash Attention pour GPU (accélération)
# pip install flash-attn --no-build-isolation
